# 🌐 Social Network - A Django Based Social Media App

این پروژه یک شبکه اجتماعی ساده است که با استفاده از فریم‌ورک قدرتمند **Django** توسعه داده شده. هدف این پروژه، پیاده‌سازی امکانات اولیه یک شبکه اجتماعی شامل عضویت کاربران، ارسال پست، لایک، کامنت و دنبال‌کردن کاربران دیگر است.

## 🚀 ویژگی‌ها

- ثبت‌نام و ورود کاربران (Authentication)
- ایجاد پروفایل برای هر کاربر
- قابلیت دنبال کردن (Follow/Unfollow)
- ارسال پست توسط کاربران (متن + تصویر)
- امکان لایک و کامنت روی پست‌ها
- صفحه فید (نمایش پست‌های کاربران دنبال‌شده)
- ویرایش اطلاعات حساب کاربری
- استفاده از Bootstrap برای طراحی ظاهر

## 🛠 تکنولوژی‌های استفاده شده

- Python 3.x
- Django 4.x
- SQLite3 (برای دیتابیس پیش‌فرض)
- Bootstrap 5
- Pillow (برای مدیریت تصاویر)
- HTML/CSS/JavaScript

## 📦 نحوه اجرا

1. پروژه را کلون کنید:

```bash
git clone https://github.com/Erfan-aminian/Social-Network.git
cd Social-Network
یک محیط مجازی ایجاد کنید و فعالش کنید:

bash
Copy
Edit
python -m venv env
source env/bin/activate  # در ویندوز: env\Scripts\activate
پکیج‌ها را نصب کنید:

bash
Copy
Edit
pip install -r requirements.txt
مایگریشن‌ها را اجرا کنید:

bash
Copy
Edit
python manage.py migrate
سرور را اجرا کنید:

bash
Copy
Edit
python manage.py runserver
بازدید در مرورگر:

cpp
Copy
Edit
http://127.0.0.1:8000/
🧪 نام کاربری ادمین (اختیاری)
در صورت وجود، یک کاربر ادمین از پیش ساخته شده نیز می‌تونید بسازید:

bash
Copy
Edit
python manage.py createsuperuser
📁 ساختار پروژه
csharp
Copy
Edit
Social-Network/
│
├── social/             # اپ اصلی شامل مدل‌ها، ویوها و قالب‌ها
├── static/             # فایل‌های CSS، JS، و تصاویر عمومی
├── media/              # فایل‌های آپلود شده توسط کاربران
├── templates/          # فایل‌های HTML
├── db.sqlite3          # پایگاه‌داده پیش‌فرض
├── manage.py
└── requirements.txt
📌 نکات
پروژه در حال توسعه است و ممکن است امکانات جدید اضافه شود.

خوشحال می‌شم اگر پیشنهادی برای بهبود پروژه دارید یا Pull Request ارسال کنید 😊

📄 مجوز
این پروژه تحت لایسنس MIT منتشر شده است. جزئیات در فایل LICENSE موجود است.

ساخته شده با ❤️ توسط Erfan Aminian

yaml
Copy
Edit

---

اگر ویژگی‌های خاص‌تری توی پروژه‌ات پیاده‌سازی کردی (مثل پیام خصوصی، سیستم نوتیفیکیشن، فالو کردن بر اساس تایید، ری‌اکت‌ها و ...) بگو تا نسخه دقیق‌تر و کامل‌تری از README برات بنویسم. آیا مایل هستی دموی پروژه یا اسکرین‌شاتی هم به README اضافه کنیم؟
